# Latitude.sh MCP Server Configuration
# Copy this file to .env.local and fill in your actual values

# Your Latitude.sh API key (required)
# Get your API key from: https://latitude.sh/account/api-keys
# You can paste the raw token or include the "Bearer " prefix; the server will add it if missing.
LATITUDE_API_KEY=your-api-key-here

# Base URL for the Latitude.sh API (optional)
# Default: https://api.latitude.sh
LATITUDE_BASE_URL=https://api.latitude.sh

# Request timeout in milliseconds (optional)
# Default: 10000 (10 seconds)
LATITUDE_TIMEOUT=10000

# Environment (optional)
# Values: development, production
# Default: development
NODE_ENV=development 